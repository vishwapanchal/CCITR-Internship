import ClientPage from './ClientPage';

export const metadata = {
  title: "APEX-X | Login",
  description: "APEX-X Login Page",
};

export default function Page(props: any) {
  return <ClientPage {...props} />;
}
